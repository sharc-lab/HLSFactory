# Data Aggregation and Packaging

This tutorial covers how to aggregate and package data from HLS synthesis and implementation flows into tabular formats (CSV, JSON) and archives.

For detailed documentation on the `CompleteHLSData` class, `DataAggregator` interface, and vendor-specific aggregation (Xilinx, Intel), see the [Data Aggregation and Packaging](../framework/data) framework guide.
